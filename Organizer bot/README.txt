This is a discord bot that ask you to fill parameters, of date and message, to send you a message on said time everyday. It could handle multiple users, as each user's info is uploaded to a mysql server

The bot could be hosted on any server, and would ask for port, host, username, password and the database its being connected to.
It will also ask for the discord bot token
